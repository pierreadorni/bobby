/**
 *  Variables d'environnement
 */

(function (window) {
  window.__env = window.__env || {};

  // URL de la webapp
  window.__env.webappUrl = 'https://bobby.nastuzzi.fr';

  // URL de l'API
  window.__env.apiUrl = 'https://bobby.nastuzzi.fr/api/v1';

}(this));
