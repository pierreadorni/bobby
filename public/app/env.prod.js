/**
 *  Variables d'environnement
 */

(function (window) {
  window.__env = window.__env || {};

  // URL de la webapp
  window.__env.webappUrl = 'https://assos.utc.fr/bobby';

  // URL de l'API
  window.__env.apiUrl = 'https:///assos.utc.fr/bobby/api/v1';

}(this));
